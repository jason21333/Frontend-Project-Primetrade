# MERN App Frontend

This is a minimal Vite + React frontend for the MERN auth example.

Setup

1. Install dependencies:

```bash
cd frontend
npm install
```

2. Start dev server:

```bash
npm run dev
```

Environment
- `VITE_API_BASE` — base URL for the backend API (defaults to `http://localhost:5000/api`)
